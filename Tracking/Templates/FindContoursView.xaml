<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:processor="clr-namespace:Tools.FlockingDevice.Tracking.Processor.OpenCv">
    <DataTemplate x:Key="FindContours" DataType="processor:FindContours">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <GroupBox Header="Pre Processed Image"
                          Grid.Column="0">
                    <Image Source="{Binding Path=PreProcessImage}" />
                </GroupBox>
                <GroupBox Header="Post Processed Image"
                          Grid.Column="1">
                    <Image Source="{Binding Path=PostProcessImage}" />
                </GroupBox>
            </Grid>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <GroupBox Header="Confidence Image"
                          Grid.Column="0">
                    <Image Source="{Binding Path=ConfidenceImage}" />
                </GroupBox>
                <GroupBox Header="Subtract Image"
                          Grid.Column="1">
                    <Image Source="{Binding Path=SubtractImage}" />
                </GroupBox>
            </Grid>

            <CheckBox Content="Fill Contours" IsChecked="{Binding Path=IsFillContours}" />
            <CheckBox Content="Draw Contours" IsChecked="{Binding Path=IsDrawContours}" />
            <CheckBox Content="Draw Center" IsChecked="{Binding Path=IsDrawCenter}" />
            <CheckBox Content="Subract Confidence Image" IsChecked="{Binding Path=IsSubtractConfidenceImage}" />

            <StackPanel>
                <Label Content="BlockSize"/>
                <Slider Value="{Binding Path=BlockSize}" Minimum="0" Maximum="100" />
                <TextBlock Text="{Binding Path=BlockSize}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="Sobel"/>
                <Slider Value="{Binding Path=Sobel}" Minimum="0" Maximum="100" />
                <TextBlock Text="{Binding Path=Sobel}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="K"/>
                <Slider Value="{Binding Path=K}" Minimum="0.00" Maximum="1.00" />
                <TextBlock Text="{Binding Path=K}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="Min Contour Area"/>
                <Slider Value="{Binding Path=MinContourArea}" Minimum="0" Maximum="1000" />
                <TextBlock Text="{Binding Path=MinContourArea}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="Min Angle"/>
                <Slider Value="{Binding Path=MinAngle}" Minimum="0" Maximum="180" />
                <TextBlock Text="{Binding Path=MinAngle}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="Max Angle"/>
                <Slider Value="{Binding Path=MaxAngle}" Minimum="0" Maximum="180" />
                <TextBlock Text="{Binding Path=MaxAngle}"/>
            </StackPanel>

            <StackPanel>
                <Label Content="Timeout"/>
                <Slider Value="{Binding Path=Timeout}" Minimum="0" Maximum="10000" />
                <TextBlock Text="{Binding Path=Timeout}"/>
            </StackPanel>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>